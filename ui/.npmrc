# Vite 8 / rolldown ship native binaries as optional deps guarded by engines. On an older Node
# npm silently skips them and the build dies with "Cannot find native binding" - fail here instead.
engine-strict=true
